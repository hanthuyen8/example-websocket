{
  "name": "openai",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "engines": {
    "node": ">=22.11.x"
  },
  "type": "module",
  "scripts": {
    "build": "tsc",
    "ws": "tsc --noEmit && npx vite-node src/websocket.ts",
    "tcp": "tsc --noEmit && npx vite-node src/tcp.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "vitest": "^2.1.8",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/ws": "^8.5.14",
    "typescript": "^5.5.3"
  }
}
